import express from "express"; import { createExpressMiddleware } from "@trpc/server/adapters/express"; import cors from "cors"; import { appRouter } from "../server/routers"; import { createContext } from "../server/_core/context"; const app = express(); app.use(cors()); app.use(express.json()); app.use("/", createExpressMiddleware({ router: appRouter, createContext })); export default app;
